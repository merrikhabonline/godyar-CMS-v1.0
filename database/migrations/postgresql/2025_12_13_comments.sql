CREATE TABLE IF NOT EXISTS comments (id BIGSERIAL PRIMARY KEY, news_id BIGINT NOT NULL, user_id BIGINT NULL, author_name VARCHAR(190) NULL, author_email VARCHAR(190) NULL, body TEXT NOT NULL, status VARCHAR(20) NOT NULL DEFAULT 'pending', created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NULL);
